{
  "name": "@hyper-odds/contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Hyper-Odds prediction markets",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "test:gas": "forge test --gas-report",
    "deploy:local": "forge script script/DeployAll.s.sol --rpc-url http://localhost:8545 --broadcast",
    "clean": "forge clean",
    "format": "forge fmt"
  },
  "files": [
    "src/**/*.sol",
    "out/**/*.json"
  ]
}